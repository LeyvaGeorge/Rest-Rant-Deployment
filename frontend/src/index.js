import React from 'react';
import ReactDOM from 'react-dom';
import App from './app'; //Might need to change to App not app

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);